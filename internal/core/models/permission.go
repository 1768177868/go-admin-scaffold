package models

// This file previously contained MenuPermission and RoleMenuPermission models
// These have been removed as we've simplified the permission system to use only the menus table
// Permissions are now managed directly through the menu.permission field and role_menus associations
