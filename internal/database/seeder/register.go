package seeder

// RegisterAll registers all available seeders
func RegisterAll(manager *SeederManager) {
	// Seeders are automatically registered via their init() functions
	// when the seeders package is imported elsewhere
}
